import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: HTTPS configuration for development should be handled
  // through the dev server script or environment variables
  // The server option is not valid in Next.js config
};

export default nextConfig;
